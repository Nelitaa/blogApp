<li>
  <h2>Post #<%= post.id %></h2>
  <p><%= post.text %></p>
  <%= render partial: 'shared/counters', locals: { post: post } %>
  <%= render partial: 'shared/comments', locals: { post: post } %>
</li>
